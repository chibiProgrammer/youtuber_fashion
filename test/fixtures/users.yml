# == Schema Information
#
# Table name: users
#
#  id            :bigint           not null, primary key
#  youtuber_name :string
#  image_url     :string
#  group_id      :integer
#  brand_id      :integer
#  age           :integer
#  infomation    :string
#  birthplace    :string
#  height        :string
#  youtube_url   :string
#  deleted_flag  :boolean
#  created_at    :datetime         not null
#  updated_at    :datetime         not null
#

# This model initially had no columns defined. If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
<% 10.times do |n| %>
user_<%= n + 1 %>:
  youtuber_name: "mizuho"
  image_url: '/assets/icon.png'
  group_id: <%= "group_#{(n + 1)}" %>
  brand_id: <%= "brand_#{(n + 1)}" %>
  age: "12"
  infomation: "info"
  birthplace: "aiueo"
  height: "150cm"
  youtube_url: 'https://www.youtube.com/watch?v=HfF38HJb5ic'
  deleted_flag: 0
<% end %>